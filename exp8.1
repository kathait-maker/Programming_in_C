Declare different types of pointers (int, float, char) and initialize them with the addresses of variables. Print the values of both the pointers and the variables they point to.


#include <stdio.h>

int main() {
    int a = 10;
    float b = 5.25;
    char c = 'X';

    int *p1 = &a;
    float *p2 = &b;
    char *p3 = &c;

    printf("Integer variable value: %d\n", a);
    printf("Integer pointer points to address: %p\n", p1);
    printf("Value using integer pointer: %d\n\n", *p1);

    printf("Float variable value: %.2f\n", b);
    printf("Float pointer points to address: %p\n", p2);
    printf("Value using float pointer: %.2f\n\n", *p2);

    printf("Char variable value: %c\n", c);
    printf("Char pointer points to address: %p\n", p3);
    printf("Value using char pointer: %c\n", *p3);

    return 0;
}
