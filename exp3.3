//a

#include <stdio.h>

int main() {
    int i, j, num = 1;

    for (i = 1; i <= 3; i++) {      // 3 rows
        for (j = 1; j <= i; j++) {  // print i numbers in each row
            printf("%d ", num);
            num++;
        }
        printf("\n");               // move to next line
    }

    return 0;
}

//b

#include <stdio.h>

int main() {
    int n = 5; // number of rows
    int i, j, num;

    for (i = 0; i < n; i++) {
        num = 1;
        for (j = 0; j <= i; j++) {
            printf("%d ", num);
            num = num * (i - j) / (j + 1);  // formula for Pascal’s triangle
        }
        printf("\n");
    }

    return 0;
}

